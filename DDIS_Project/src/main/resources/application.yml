
server:
  port: 8080

spring:
  application:
    name: DDIS_Project

  security:
    user:
      name: none
      password: none

  ## jpa config
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        '[format_sql]': true

  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3306/ddis       #각자 수정
    username: DDIS                                #각자 수정
    password: DDIS                                #각자 수정

